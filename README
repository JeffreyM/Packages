# Simple, quick install for new ec2 instances
sudo apt-get update
sudo apt-get upgrade
sudo apt-get -y install git-core openssl libssl-dev
cd ~
git clone git://github.com/JeffreyM/Packages.git
cd ./packages
./install-node.sh


